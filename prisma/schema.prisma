generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum NewsArticleStatus {
  DRAFT
  PUBLISHED
}

enum DocumentResourceStatus {
  DRAFT
  PUBLISHED
}

enum CalendarEventType {
  JOURNEE
  CONVOCATION
  INSCRIPTION
  RESULTAT
}

model SiteSettings {
  id                 Int      @id
  siteName           String
  shortName          String
  description        String
  season             String
  email              String
  phone              String
  organization       String
  publicationDirector String
  dataContact        String
  addressLine1       String
  addressLine2       String
  postalCode         String
  city               String
  officeHours        String
  affiliations       String
  partners           String
  facebookUrl        String
  instagramUrl       String
  updatedAt          DateTime @updatedAt
  createdAt          DateTime @default(now())
}

model NewsArticle {
  id          String            @id @default(cuid())
  slug        String            @unique
  title       String
  excerpt     String
  content     String
  category    String
  readTime    String
  status      NewsArticleStatus @default(DRAFT)
  featured    Boolean           @default(false)
  publishedAt DateTime?
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt

  @@index([status, publishedAt(sort: Desc)])
}

model DocumentResource {
  id          String                 @id @default(cuid())
  title       String
  category    String
  format      String
  description String
  fileUrl     String
  competitionId String?
  status      DocumentResourceStatus @default(DRAFT)
  createdAt   DateTime               @default(now())
  updatedAt   DateTime               @updatedAt

  @@index([competitionId])
  @@index([status, updatedAt(sort: Desc)])
}

model CalendarEvent {
  id            String            @id @default(cuid())
  competitionId String?
  title         String
  type          CalendarEventType
  date          DateTime
  location      String
  published     Boolean           @default(false)
  sortOrder     Int               @default(0)
  createdAt     DateTime          @default(now())
  updatedAt     DateTime          @updatedAt

  @@index([competitionId])
  @@index([published, date(sort: Asc)])
  @@index([date(sort: Asc), sortOrder])
}

model ClubResource {
  id        String   @id @default(cuid())
  ffttId    String?  @unique
  name      String
  city      String
  venue     String
  audience  String
  tables    Int      @default(0)
  contact   String
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([active, city])
}

model FfttClubLicenseeStat {
  id             String   @id @default(cuid())
  clubFfttId     String   @unique
  clubName       String
  city           String
  licenseeCount  Int      @default(0)
  syncError      String?
  syncedAt       DateTime @default(now())
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([syncedAt])
  @@index([city])
}

model CommitteeMemberResource {
  id        String   @id @default(cuid())
  name      String
  role      String
  initials  String
  mission   String
  area      String
  imageUrl  String?
  sortOrder Int      @default(0)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([active, sortOrder])
}

model TechnicalStaffMemberResource {
  id        String   @id @default(cuid())
  name      String
  role      String?
  initials  String
  mission   String?
  area      String?
  imageUrl  String?
  sortOrder Int      @default(0)
  active    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([active, sortOrder])
}
